# แผนปรับปรุง + ตรวจสอบทั้งเล่ม — "Python for Quant Traders"

> เอกสารนี้คือ **แผนงาน** ไม่ใช่การแก้ไข — ใช้เป็นใบสั่งงานสำหรับรอบปรับปรุงเล่ม
> ขอบเขต: `docs/python-part0..6.html` + `docs/python-appendix.html` (บทที่ 1–35 + glossary)
> ต้นทาง: ย้ายเข้า branch นี้จาก `claude/grid-trading-book-review-dve69o` (PR #21)

---

## 0. โจทย์ที่ผู้ใช้เคาะแล้ว (หลักการกำกับทุกการตัดสินใจ)

| ข้อ | สิ่งที่ต้องการ | สิ่งที่ **ห้าม** ทำ |
|---|---|---|
| 1 | อธิบายให้ง่าย เข้าใจ **logic** ได้ | ❌ ลดความเข้มข้น/ตัดเนื้อหาออกเพื่อให้ง่าย |
| 2 | สอนให้เข้าใจ **การทำงานของ function** ไม่ใช่ท่องว่าพิมพ์อะไร | ❌ ให้โค้ดสำเร็จแล้วบอก "รันได้แล้ว จบ" |
| 3 | ต่อยอด**คิดเป็น logic** เองได้ | ❌ สอนเป็นสูตรลอกตาม (recipe) |
| 4 | **เป็นมิตรกับมือใหม่** | ❌ สมมติว่าผู้อ่านรู้อยู่แล้ว |
| 5 | **มือโปรต้องไม่เบื่อ** | ❌ อธิบายซ้ำซากจนคนมีพื้นข้ามทั้งบท |

### 🔑 หลักคิดที่ทำให้ข้อ 4 กับ 5 ไม่ขัดกัน — "ชั้นการอ่าน" (layering)

"มือใหม่เข้าใจ" กับ "มือโปรไม่เบื่อ" **ไม่ใช่ trade-off** ถ้าเลิกมองว่าความง่ายคือการ*เจือจาง* แล้วมองว่าความง่ายคือการ*เพิ่มชั้นที่ข้ามได้*

```
ชั้น 1  🚶 อ่านโค้ดเป็นภาษาคน      ← มือใหม่เริ่มที่นี่ · มือโปรกวาดตาผ่าน 2 วิ
ชั้น 2  🤖 แกะทีละขั้น / ทำไมบรรทัดนี้จำเป็น
ชั้น 3  🧠 ทำไมไม่ทำแบบอื่น · trade-off · edge case  ← มือโปรมาอ่านชั้นนี้
ชั้น 4  ⚙️ กับดักที่มือโปรเองก็พลาด · ตัวเลข/ต้นทุนจริง
```

- มือใหม่: ชั้น 1→2 พอเดินได้ · ชั้น 3–4 ติดป้าย **[รอบสอง]** ไว้ให้ข้ามได้อย่างสบายใจ
- มือโปร: ข้ามชั้น 1 แต่**ชั้น 3–4 คือของที่เขาหาอ่าน** — นี่คือส่วนที่กันความเบื่อ
- ⚠️ ความเบื่อของมือโปรเกิดจาก "ไม่มีชั้น 3–4" มากกว่า "มีชั้น 1" — ดังนั้นทางแก้คือ **เติมชั้นลึก ไม่ใช่ตัดชั้นง่าย**

**กฎเหล็กของรอบปรับปรุงนี้: เพิ่มได้ ห้ามลด** — ทุก finding ต้องแก้ด้วยการ *เติมชั้นอธิบาย* หรือ *จัดชั้นใหม่* ห้ามแก้ด้วยการตัดเนื้อหาเทคนิคออก

---

## 1. ของดีที่เล่มนี้มีอยู่แล้ว (baseline — อย่า "ปรับปรุง" ทิ้ง)

ตรวจแล้วเล่มนี้**มีกลไกการสอนที่ตรงกับโจทย์ผู้ใช้อยู่แล้ว** ไม่ต้องประดิษฐ์ใหม่:

| กลไก (CSS class) | ทำหน้าที่ | ตรงกับโจทย์ข้อ |
|---|---|---|
| `.read-aloud` | 🚶 "อ่านโค้ดบรรทัดนี้เป็นภาษาคน" เช่น `price = price + 10` → *"เอาค่า price เดิม บวก 10 แล้วใส่กลับเข้า price — บรรทัดนี้ไม่ใช่สมการคณิตศาสตร์"* | **1, 2** |
| `.ai-decode` | 🤖 แกะโค้ดหนึ่งบรรทัดเป็นขั้น ๆ + สอน**สูตรอ่าน** (เช่น list comprehension: "อ่านครึ่งหลังก่อน แล้วย้อนมาครึ่งแรก") | **2, 3** |
| `.key-idea` | 💡 แก่นบทใน 2–3 บรรทัด ก่อนลงรายละเอียด (มี 6–8 กล่อง/Part สม่ำเสมอทั้งเล่ม ✓) | 1 |
| `.output` | แสดงผลลัพธ์จริงที่โค้ดพิมพ์ออกมา | 2 |
| `.running-ex` | ตัวอย่างต่อเนื่อง (vol mean-reversion) ที่ไล่ยาวทั้งเล่ม | 3 |
| `<details>` + `.ans` | แบบฝึกหัด + เฉลย — **มี 5 ข้อ/Part สม่ำเสมอทั้งเล่ม ✓** | 3 |
| `.fm` | บล็อกโค้ด (สะอาด — ตรวจแล้วไม่มีร้อยแก้วไทยยาวปนใน monospace ✓) | — |

จุดแข็งเชิงเนื้อหาที่ต้องรักษา:
- **`.shift(1)` = แกนร้อยเล่ม** — สอนตั้งแต่ Part I (log return) → ยกเป็น "กฎเหล็ก" Part IV → Part V มีทั้งบท (28.1 Red Flag #1) + โค้ด audit อัตโนมัติ + แยก `shift(1)` ถูก vs `shift(-1)` ผิด (ใช้ข้อมูลอนาคต) — ระดับนี้ดีกว่าคอร์สทั่วไปมาก
- **บทที่ 14 Cointegration** — Engle-Granger + Johansen + z-score signal + ตาราง "correlation สูง vs cointegrated" ครบ
- Part V (AI-Assisted Coding) เป็นของหาไม่ได้ในตำราเล่มอื่น — เก็บไว้เต็ม ๆ

---

## 2. ผลตรวจเบื้องต้น — สิ่งที่พบแล้ว

### 🔴 finding ใหญ่สุด: "หน้าผา scaffolding" หลัง Part I

นับกลไกอธิบายต่อ Part (ตัวเลข = จำนวนกล่องในไฟล์):

| กลไก | part0 | **part1** | part2 | part3 | part4 | part5 | part6 |
|---|---|---|---|---|---|---|---|
| `.read-aloud` 🚶 | 0 | **9** | **0** | 1 | **0** | **0** | **0** |
| `.ai-decode` 🤖 | 0 | **6** | 2 | 2 | **0** | **0** | **0** |
| `.key-idea` 💡 | 8 | 7 | 8 | 6 | 8 | 6 | 7 |
| `.output` | 0 | 8 | 1 | 6 | 16 | 15 | 9 |
| แบบฝึกหัด | 4 | 5 | 5 | 5 | 5 | 5 | 5 |

**อ่านตารางนี้:** โค้ดใน Part II–VI **ยากขึ้นเรื่อย ๆ** (Johansen test, event-driven backtest, `asyncio.gather`, WebSocket reconnect) แต่ชั้นอธิบาย 🚶/🤖 **หายไปเกือบหมด** — มือใหม่ที่รอดจาก Part I มาได้ จะไปตกหน้าผาที่ Part II

ที่แย่กว่านั้น: Part I มีตาราง **"ตารางเสียงอ่าน — ใช้ตลอดทั้งเล่ม"** (`python-part1.html:171`) → เล่มนี้**สัญญาไว้เองแล้วไม่ทำตาม** (`.read-aloud` = 0 ใน 4 Part ท้าย)

✅ ข่าวดี: finding นี้แก้ได้ตรงตามกฎ "เพิ่มได้ ห้ามลด" 100% — เป็นการ *เติม* กล่องอธิบาย ไม่แตะโค้ดหรือเนื้อหาเทคนิคเลย

### 🟠 ช่องว่างเนื้อหาที่ยืนยันแล้ว

**A. Visualization — การกระจายตัว (จากคำถามผู้ใช้ 2 รอบ)**

| หัวข้อ | สถานะ |
|---|---|
| Scatter plot + correlation | ✅ มี — บทที่ 10.3 (`ax.scatter` + `.corr()` โชว์บนกราฟ) |
| Correlation matrix | ✅ มี — `df.pct_change().corr()` |
| Cointegration | ✅ มี — บทที่ 14 เต็มบท |
| **Histogram** | ✅ **เพิ่มแล้ว** — บทที่ 10.4 (`plt.hist(density=True)`) |
| **Box plot** | ✅ **เพิ่มแล้ว** — บทที่ 10.5 เทียบ BTC/ETH + ai-decode อธิบาย 5 ตัวเลขในกล่อง |
| **Bell curve พล็อตด้วยโค้ด** | ✅ **เพิ่มแล้ว** — บทที่ 10.4 overlay `stats.norm.pdf()` ทับ histogram + ตารางนับวันที่เกิน kσ จริง vs ที่ Normal ทำนาย (4σ เกิน 100 เท่า) + QQ-plot ในชั้นมือโปร |

→ ทั้ง 3 ข้อเป็นกลุ่มเดียวกัน = **"มองรูปร่างการกระจายของ returns"** ควรเป็นหัวข้อใหม่ก้อนเดียว

**B. Python ที่ยังขาด** (เทียบกับที่เล่มนี้ *ใช้* โค้ดระดับ production)

| หัวข้อ | สถานะ | ความสำคัญ |
|---|---|---|
| `venv` / virtualenv / `requirements.txt` | ✅ **เพิ่มแล้ว** — บทที่ 6.1 ขยายเป็น 5 ขั้น + กล่อง ❌ อาการที่เกิดถ้าข้าม + ชั้นมือโปร (`.gitignore`, VS Code interpreter, `sys.prefix`) | — |
| `pytest` + โครงไฟล์ test | 🟡 มีเมล็ด — Part V ใช้ `assert` (7 จุด) แต่ไม่มีบทสอน framework/โครงโปรเจกต์ | 🔴 เล่มสอน "audit AI code" แต่ไม่สอนวิธี test เป็นระบบ |
| context manager (`with`) | ✅ **เพิ่มแล้ว** — หัวข้อใหม่ 8.5 วางไว้ก่อนบทที่ 9 ที่เริ่มอ่านไฟล์ และโยงล่วงหน้าไป `async with` ของ Part VI | — |
| decorator | 🟡 พบ 1 ครั้ง | 🟠 จำเป็นตอนอ่าน lib จริง |
| generator / `yield` | 🟡 พบ 1 ครั้ง | 🟡 |
| `logging` | ✅ มี 26 จุด (Part VI) | — |
| type hints | ✅ มี (`typing` 4 ไฟล์) | — |

**C. โครงสร้าง**
- ✅ เลขบท 1–35 ต่อเนื่องไม่มีช่อง (P0:1–5, P1:6–10, P2:11–17, P3:18–21, P4:22–25, P5:26–30, P6:31–35)
- ✅ nav prev/next ครบทุกไฟล์ · asset `vendor/` (Sarabun + KaTeX) ครบ
- ✅ **appendix เข้าถึงได้แล้ว** — เพิ่มแถบ "📘 สารบัญทั้งเล่ม · 📎 คำศัพท์ Quant Trading" ท้ายทุก Part (8 ไฟล์)
- ✅ **มีหน้าสารบัญของเล่มแล้ว** — `python-for-quant-traders.html` (การ์ดต่อ Part + reading path 🟢/🔵/🟠 + ผังลำดับ + กล่องมาตรฐานการตรวจ) และเพิ่มเข้า `docs/index.html` เป็นหมวด "เล่มคู่ขนาน — ลงมือเขียนโค้ด" คู่กับเล่ม math
- ✅ **มี reading-path banner ครบทั้ง 7 Part** (🟢/🔵) — เขียนเฉพาะเจาะจงแต่ละ Part ว่าให้อ่านอะไร ข้ามอะไร และมองหากล่องแบบไหน ไม่ใช่ข้อความกลาง ๆ ซ้ำกัน

---

## 3. แผนตรวจสอบทั้งเล่ม — คณะผู้เชี่ยวชาญ 6 เลนส์

ตาม `craft-standard.md` (รีวิวหลายเลนส์อิสระจับ error คนละแบบ) ปรับเลนส์ให้เข้ากับ**หนังสือสอนเขียนโค้ด** — เดิมออกแบบไว้สำหรับหนังสือทฤษฎี จึงเพิ่มเลนส์ที่ตำราทฤษฎีไม่มีคือ "โค้ดรันได้จริงไหม"

รันทีละเลนส์ ทั้ง 8 ไฟล์ บันทึก finding เป็น `[เลนส์][ความรุนแรง] ไฟล์:บรรทัด — อาการ → ทางแก้`
ความรุนแรง: **C**ritical (ผิด/รันไม่ได้/หลงทาง) · **M**oderate (เข้าใจผิดได้) · **N**it (ขัดตา)

### เลนส์ 1 — 🐍 โค้ดต้องรันได้จริง *(สำคัญสุด — ตำราโค้ดที่รันไม่ได้ = พังทั้งเล่ม)*
- **สกัดทุก snippet ออกมารันจริง** ในสภาพแวดล้อมสะอาด — ไม่ใช่อ่านตรวจด้วยตา
- เช็ค: import ครบไหม · ตัวแปรถูกนิยามก่อนใช้ไหม (snippet ต่อเนื่องข้ามกล่อง) · เวอร์ชัน API ยังถูกไหม (`pandas` deprecation เช่น `.append()`, `fillna(method=)` · `statsmodels` signature)
- `np.random.seed` ครบทุกที่ที่โชว์ `.output` ไหม — **ถ้า output ในหนังสือ reproduce ไม่ได้ ผู้อ่านจะคิดว่าตัวเองพิมพ์ผิด**
- ตัวเลขใน `.output` ตรงกับที่โค้ดคำนวณจริงไหม (บทเรียนจากเล่ม math: เคยเจอ Gamma=0.0151 ไม่ตรงโค้ด)
- ตัวอย่างที่ยกว่า "ผิด" (เช่น `shift(-1)`) ติดป้ายชัดว่าห้ามลอกไปใช้ไหม

### เลนส์ 2 — 📐 ความถูกต้องเชิง quant / สถิติ
- log return vs simple return ใช้ถูกที่ไหม · annualize ด้วย √252 ถูกบริบทไหม
- lookahead / survivorship / p-hacking — อ้างครบและไม่ขัดกันเองข้าม Part ไหม
- ADF/Johansen ตีความ p-value และ null hypothesis ถูกไหม (จุดที่ตำราพลาดบ่อยที่สุด)
- VaR/CVaR, Kelly, Sharpe — สูตรและคำเตือน sample size ถูกไหม
- fee/slippage/funding ในบทที่ 25 สมจริงไหม

### เลนส์ 3 — 🟢 มือใหม่ตามได้ไหม *(โจทย์ข้อ 1–4)*
- ไล่อ่านแบบ **"คนไม่เคยเขียนโค้ด"** ทีละบรรทัด หยุดทุกจุดที่ต้อง "เดา"
- ทุกแนวคิดใหม่ถูกอธิบาย**ก่อน**ใช้ไหม (พบแล้ว: `with` ถูกใช้ใน Part VI ก่อนสอน)
- โค้ดที่ยาว >10 บรรทัด มี 🚶 หรือ 🤖 กำกับไหม → **นี่คือเช็คลิสต์หลักของ finding ข้อ 2**
- error message ที่มือใหม่จะเจอจริง มีสอนอ่านไหม (`KeyError`, `SettingWithCopyWarning`, `NaN` เงียบ ๆ)
- ศัพท์ที่โยงไป glossary ได้ — ทำ cross-ref ไหม

### เลนส์ 4 — 🔵 มือโปรได้อะไร *(โจทย์ข้อ 5 — กันเบื่อ)*
ตรวจว่าแต่ละบทมี **"ของที่คนเขียนโค้ดเป็นแล้วยังไม่รู้"** อย่างน้อย 1 ชิ้น:
- **ทำไมไม่ทำแบบอื่น** — vectorized vs event-driven ต้องมีเหตุผลเชิงวิศวกรรม (ไม่ใช่ "ทำได้ 2 แบบ")
- **กับดักที่มือโปรก็พลาด** — pandas index alignment เงียบ ๆ · float กับ order size/tick size · timezone ใน timestamp exchange · `asyncio` blocking call หลุดเข้า event loop
- **ตัวเลขจริงจากสนาม** — latency, rate limit, ต้นทุนต่อ round trip
- **heuristic ระดับโต๊ะ** — "Sharpe > 2 ให้สงสัยว่าลืมต้นทุน" ประเภทนี้กระจายพอไหม
- ถ้าบทไหน**ไม่มีของชั้น 3–4 เลย** = จุดที่มือโปรจะเบื่อ → บันทึกเป็น finding

### เลนส์ 5 — 🧵 ความสอดคล้อง + คำสัญญา
- **คำสัญญาในเล่มถูกทำจริงไหม** (พบแล้ว: "ตารางเสียงอ่าน ใช้ตลอดทั้งเล่ม" แต่ไม่ทำ) — ไล่ทุกประโยคที่บอกว่า "Part ต่อไปจะ..." / "ใช้ทั้งเล่ม" แล้วเช็คว่าเกิดขึ้นจริง
- naming/notation เดียวกันทั้งเล่มไหม (`returns` vs `ret` vs `rets`, `signal` vs `position`)
- running example (vol mean-reversion) ต่อเนื่องจริงไหม หรือหลุดกลางทาง
- cross-ref: appendix เข้าถึงได้ไหม · โยงเล่ม `math-*` ที่มีเนื้อหาเดียวกันไหม (**Write Once** — เช่น cointegration มีทั้งที่นี่และ `math-part8`)
- **Part 0 ↔ Part II ซ้ำกันแค่ไหน** (สถิติทั้งคู่) — ควรเป็น "แนวคิด → โค้ด" ไม่ใช่เล่าใหม่

### เลนส์ 6 — 🇹🇭 ภาษา / typography / HTML
ตาม `style-guide.md`:
- รายการ ≥2 ข้อ ใช้ `<ul>/<ol>` จริง ไม่ใช่ `•` + `<br>` (ตัวการทำบรรทัดไทยตัดกลางคำ)
- `.fm` เก็บเฉพาะโค้ด ✅ ผ่านแล้ว (ตรวจ 0 จุด)
- เว้นวรรครอบคำอังกฤษ · em-dash ≤1/ประโยค · ประโยค ≤~2 บรรทัดจอ
- Thai line-break `<script>` ท้ายไฟล์ครบทุกไฟล์ไหม (ห้ามแก้เนื้อใน)
- HTML entity ใน code (`&lt;` `&gt;` `&amp;`) escape ถูกทุกที่ไหม — commit ก่อนหน้าเคยแก้เรื่องนี้ อาจมีตกค้าง

---

## 4. Backlog ปรับปรุง (สั่งงานได้เลย)

### P0 — ปิดหน้าผา scaffolding *(ตอบโจทย์ข้อ 1–4 ตรงที่สุด)*
1. เติม `.read-aloud` ให้โค้ดสำคัญใน **Part II–VI** ตามที่ Part I สัญญาไว้ — เป้า ≥6 กล่อง/Part เน้นบรรทัดที่ "อ่านไม่ออกด้วยสายตา":
   - Part II: `coint()`, `johansen`, `spread.shift(1)`, z-score rolling
   - Part III: `super().__init__()`, `@abstractmethod`, `self` ครั้งแรก
   - Part IV: `signal.shift(1) * returns`, `(in_trade != in_trade.shift()).cumsum()` ← บรรทัดนี้มือใหม่อ่านไม่ออกแน่นอน
   - Part VI: `async def`, `await`, `asyncio.gather()`, `async with`
2. เติม `.ai-decode` ให้ one-liner หนัก ๆ ใน Part IV–VI (ปัจจุบัน 0)
3. ใส่ **reading-path banner 🟢/🔵** ทุก Part + ติดป้าย **[รอบสอง]** กล่องชั้น 3–4 → ให้ทั้งสองกลุ่มรู้ว่าควรอ่าน/ข้ามอะไร (นี่คือกลไกที่ทำให้ "ไม่เบื่อ" ทำงานได้จริง)

### P1 — ปิดช่องว่างเนื้อหา
4. **หัวข้อใหม่: "รูปร่างของการกระจาย"** (เสียบใน Part I บทที่ 10 เป็น 10.4 หรือแตกเป็น 10.4–10.6)
   - histogram ของ daily returns (`plt.hist`, bins, density)
   - overlay bell curve `scipy.stats.norm.pdf(x, μ, σ)` ทับ histogram → **เห็นด้วยตาว่า returns จริงไม่ normal** (หางหนากว่า) = สะพานตรงเข้าบทที่ 11 fat tail
   - box plot เทียบ returns หลายเหรียญ (median/IQR/outlier)
   - 🔵 ชั้นลึกสำหรับมือโปร: QQ-plot, ทำไม histogram หลอกตาเมื่อเปลี่ยน bins
5. **บทใหม่/หัวข้อ: สภาพแวดล้อมโปรเจกต์** — `venv`, `requirements.txt`, ทำไมห้าม `pip install` ลง global (เสียบต้น Part I บทที่ 6)
6. **ขยายเรื่อง test** — `pytest` + โครงไฟล์ + ทำไม assert ลอย ๆ ไม่พอ (ต่อจากเมล็ดใน Part V บทที่ 28)
7. เติม `with` (context manager) ปกติ **ก่อน** Part VI ใช้ `async with` + decorator/generator แบบสั้นพอใช้งาน

### P2 — โครงสร้าง/การเข้าถึง
8. ทำหน้า index ของเล่ม (`python-for-quant-traders.html`) แบบเดียวกับ `math-for-options-guide.html`
9. เพิ่มเล่มนี้เข้า `docs/index.html`
10. ลิงก์ `python-appendix.html` (glossary) จากทุก Part + cross-ref ศัพท์
11. cross-ref ↔ เล่ม `math-*` ตามกฎ Write Once (cointegration, OLS, optimization ทับกัน)

### P3
12. render PDF ใหม่ทุกไฟล์ (`scripts/render-pdf.mjs`) หลังแก้เสร็จ — PDF ปัจจุบันเป็นของก่อนแก้

---

## 5. ลำดับงาน

| Phase | งาน | commit |
|---|---|---|
| **R0** | ✅ เสร็จ — สำรวจ + เขียนแผนนี้ | `docs: แผนปรับปรุง+ตรวจสอบเล่ม Python` |
| **R1** | ✅ เสร็จ — เลนส์ 1 รันโค้ดทั้งเล่ม (ดู §7) | `fix(python): โค้ดรันได้จริง — 8 บั๊กจริง` |
| **R3-spike** | ✅ เสร็จ — Part VI (async debut + ผ่าบล็อก 223 บรรทัด) + port CSS + คืนสคริปต์ตัดคำไทยทั้งเล่ม | `docs(python): spike R3 ปิดหน้าผา Part VI` |
| **R3-part2** | ✅ เสร็จ — 16/29 (55%) · coint/Johansen/minimize/build_spread | `docs(python): เติมชั้นอธิบาย Part II` |
| **R2** | เลนส์ 2 + 5 (ความถูกต้อง + สอดคล้อง/คำสัญญา) | `fix(python): ความถูกต้องเชิง quant + consistency` |
| **R3** | เลนส์ 3 + 4 → **Backlog P0** (หน้าผา scaffolding + ชั้นการอ่าน) — ก้อนใหญ่สุด แยก commit ต่อ Part | `docs(python): เติมชั้นอธิบาย Part N (🚶/🤖/banner)` |
| **R4** | Backlog P1 (histogram/bell curve/box plot · venv · pytest · with) | `docs(python): เติมหัวข้อที่ขาด — distribution viz, env, testing` |
| **R5** | เลนส์ 6 + Backlog P2 (typography + index/glossary/cross-ref) | `docs(python): typography + index + cross-ref` |
| **R6** | render PDF + อัปเดตแผนนี้ให้ตรงผลจริง | `docs(python): rebuild PDFs` |

ทุก Phase: commit แยก + push เข้า `claude/python-book-j7wvng` (PR #21 อัปเดตอัตโนมัติ)

---

## 6. เกณฑ์ "ตรวจจบ"

- [ ] **ทุก snippet ในเล่มรันได้จริง** และ `.output` reproduce ได้ (มี seed)
- [ ] ไม่มีแนวคิดใดถูกใช้ก่อนสอน — ไล่ตรวจตามลำดับการอ่านจริง
- [ ] โค้ดยาว >10 บรรทัดทุกก้อนมีชั้น 🚶 หรือ 🤖 กำกับ
- [ ] ทุกบทมี "ของสำหรับมือโปร" (ชั้น 3–4) ≥1 ชิ้น
- [ ] `.read-aloud` ≥6 กล่อง/Part ทุก Part ที่มีโค้ด — คำสัญญา "ใช้ตลอดทั้งเล่ม" เป็นจริง
- [ ] ทุก Part มี reading-path banner 🟢/🔵 + กล่องลึกติดป้าย [รอบสอง]
- [ ] histogram · bell curve (โค้ด) · box plot มีในเล่ม
- [ ] glossary เข้าถึงได้จากทุก Part · เล่มมีหน้า index · อยู่ใน `docs/index.html`
- [ ] **เกณฑ์สูงสุด:** มือใหม่ที่ไม่เคยเขียนโค้ด เดินจากบทที่ 1 ถึง 35 จบได้โดยไม่ต้องไปหาแหล่งอื่น **และ** คนเขียนโค้ดเป็นแล้วยังได้ของใหม่ทุกบท

---

## 7. ผล Phase R1 — รันโค้ดทั้งเล่มจริง (เสร็จแล้ว)

**วิธี:** สกัดบล็อก `<div class="fm">` ทั้ง 178 บล็อกออกมารันจริงบน Python 3.11 + pandas 3.0.5 / numpy 2.4.6 / statsmodels 0.14.6 แบบ **cumulative namespace ต่อไฟล์** (เลียนแบบผู้อ่านที่ไล่รันทีละบล็อกจากต้นบท)

### ผลรวม

| ไฟล์ | ก่อนแก้ | หลังแก้ | หมายเหตุ |
|---|---|---|---|
| part1 | 43/49 | **45/49** | 4 ที่เหลือเป็น false positive ทั้งหมด |
| part2 | 24/29 | **29/29 ✅** | แก้บั๊กจริง 3 กลุ่ม |
| part3 | 11/19 | **15/19** | 4 ที่เหลือเป็น false positive |
| part4 | 21/23 | **23/23 ✅** | แก้บั๊กจริง 1 |
| part5 | 19/21 | 19/21 | 2 ที่เหลือเป็น artifact ของ harness |
| part6 | — | — | ⏳ ยังไม่ได้รัน (บล็อก live-data/WebSocket ค้างรอ network) |

### 🔴 บั๊กจริงที่แก้แล้ว (8 จุด)

| # | ที่ | อาการ | แก้เป็น |
|---|---|---|---|
| 1 | **part2 บทที่ 13 (OLS)** | `sm.add_constant(numpy array)` → `model.params['const']` พัง `IndexError` เพราะ params ไม่มีชื่อคอลัมน์ | ห่อเป็น `pd.DataFrame({"log_bybit": ...})` + เพิ่ม 🚶 read-aloud อธิบายว่า**ชื่อมาจาก pandas ไม่ใช่ statsmodels** + กล่องกับดัก `print(type(model.params))` |
| 2 | **part2 บทที่ 13** | ข้อมูลจำลองเป็น random walk **สองเส้นที่ไม่เกี่ยวกัน** → β=−0.15, R²=0.024, p=0.014 ซึ่ง**ขัดกับตารางเกณฑ์ 13.2 ของตัวเองทุกข้อ** (ตารางบอก β≈1.0, R²>0.85, p<0.001) | ให้ spread เป็น **OU process** (แพตเทิร์นเดียวกับที่หนังสือใช้เองในบทที่ 12) → β=0.981, R²=0.990, p<0.001 ✓ ครบเกณฑ์ |
| 3 | **part2 บทที่ 13.3** | RollingOLS ซ้ำบั๊กเดียวกัน | แก้ + เพิ่ม output block + read-aloud |
| 4 | **part2 บทที่ 14 (half-life)** | พิมพ์ `hold ไม่เกิน 0 วัน` (ไร้ความหมาย) เพราะ spread เป็น noise → half-life 0.2 วัน | หลังแก้ข้อมูล → half-life **3.8 วัน** → window 11 วัน / hold 8 วัน + เตือนว่า half-life <1 วัน = pair นี้ไม่ควรเทรด ไม่ใช่โค้ดพัง |
| 5 | **part1 บทที่ 9→10** | `df` ถูกกำหนดใหม่ในเฉลยแบบฝึกหัด (BTC/ETH/GOLD) แล้วบทที่ 10 ยังเรียก `df["close"]` → ผู้อ่านที่ไล่ตามลำดับเจอ `KeyError` ทั้งกราฟ 3 panel และ candlestick | เปลี่ยนตัวแปรในเฉลยเป็น `prices` |
| 6 | **part1 บทที่ 9** | กล่อง "ไฟล์ btc_daily.csv มาจากไหน?" อ้างว่า *"ตัวอย่างด้านล่างรันได้ทันทีโดยไม่ต้องดาวน์โหลด"* แต่บล็อกถัดไป `read_csv` จริง · และ **ทั้ง 2 วิธีที่แนะนำให้คอลัมน์ไม่ตรงกับโค้ด** (yfinance ให้ `Close` ตัวใหญ่ + index `Date` · CoinGecko ให้ `snapped_at,price`) → มือใหม่เจอ `KeyError: 'close'` ที่จุดสัมผัสข้อมูลจริงครั้งแรก | เพิ่มบล็อกสร้าง CSV จำลอง (ทำให้คำสัญญาเป็นจริง) + สอนขั้น normalize คอลัมน์จริง (lower/flatten MultiIndex) + 🧭 นิสัย `print(df.columns.tolist())` ก่อนใช้ทุกครั้ง |
| 7 | **part3 บทที่ 18–20** | `pd.` ใช้ตั้งแต่บรรทัด 214 แต่ `import pandas as pd` โผล่ครั้งแรกบรรทัด 1012 · `sm`/`RollingOLS` ไม่เคย import | เพิ่ม import header ที่บล็อกแรกของ Part + เติมคอมเมนต์ `# ต้อง import เพิ่ม:` ให้ครบ |
| 8 | **part4 บทที่ 25** | `backtest_with_realistic_costs()` สร้าง `equity_net`/`equity_gross` แต่ `compute_metrics()` อ่าน `result["equity"]` → `KeyError: 'equity'` | เพิ่ม `df["equity"] = df["equity_net"]` + คอมเมนต์ว่าทำไมต้องวัดจาก net |

### ✅ False positive ที่ตรวจแล้วว่า "ไม่ใช่บั๊ก" (สำคัญ — อย่าไปแก้)

- **part1:188, 404** — `if signal = 1:` และ `if z > 1.0 and < 3.0:` เป็น **ตัวอย่างที่ตั้งใจให้ผิด** มีป้าย ❌ กำกับ พร้อมเวอร์ชันถูกข้าง ๆ = การสอนที่ดี
- **part3:277** — `m.annual_vol()` ที่ตั้งใจให้ TypeError เพื่อสอนว่า `@property` ทำให้ไม่ต้องใส่ `()`
- **part3:109, 119, 133** — บล็อก "พรีวิว" ต้นบทที่อ้างคลาสซึ่งนิยามทีหลัง (โครงหนังสือ ไม่ใช่บั๊ก)
- **part5:412, 756** — `inspect.getsource()` ใช้กับโค้ดที่ผมรันผ่าน `exec` ไม่ได้ = artifact ของ harness

> ⚠️ **บทเรียนสำคัญจาก R1:** รอบแรกผมเกือบ "แก้" โค้ดที่ถูกอยู่แล้ว — regex ลบ HTML tag (`<[^>]+>`) ของสคริปต์สกัด **กิน format spec ของ f-string** เพราะ `{'x':<28} {y:>10.2f}` ตรงกลางหน้าตาเหมือน HTML tag ทำให้ดูเหมือน Part IV มี print พัง 4 จุด · **กฎที่ได้: อ่าน HTML ต้นฉบับยืนยันทุก finding ก่อนแก้เสมอ** ห้ามเชื่อ output ของเครื่องมือตัวเอง

### ✅ finding ที่ปิดแล้วในรอบต่อมา

**บทที่ 21 (capstone) — `trades: 0`** · ต้นเหตุ 2 ชั้นทับกัน:
1. `fit(df.iloc[:60])` กับ `window=60` → ข้างในมี **สอง window ซ้อน** (RollingOLS 60 แล้ว `rolling(60)` อีกชั้น) → z-score เป็น `NaN` ทั้งชุด → `signal()` คืน `"HOLD"` ตลอด
2. `signal(data)` **ไม่เคยใช้ `data` ที่ส่งเข้ามา** — อ่านจาก z-score ที่ cache ไว้ตอน `fit()` ครั้งเดียว จึงไม่อัปเดตตามเวลาเลย

**แก้โดยรื้อสถาปัตยกรรมให้ถูก:** แยก `fit()` (เรียน alpha/beta/mu/sd จากอดีต ทำนาน ๆ ครั้ง) ออกจาก `signal()` (ให้คะแนน**แท่งล่าสุด**ที่ส่งเข้ามา ทำได้ทุก timestep) — เป็นแพตเทิร์นที่ถูกต้องของ live trading และทำให้ refit กลางทางง่ายด้วย · เพิ่ม OU spread + ยืด simulate เป็น 240 วัน + refit ทุก 20 วัน → **`trades: 6`** และเพิ่ม `.output` (เดิม**ไม่มี** `.output` เลย ซึ่งเป็นเหตุที่บั๊กนี้รอดมาได้)

⚠️ **แต่เปิด finding ใหม่ที่ลึกกว่า:** ทุกเทรดเป็น `LONG Bybit` **ขาเดียว** เพราะ `system.step("Bybit", price, window)` ส่งแค่ symbol เดียว → **P&L มาจากทิศทางราคา Bybit ไม่ใช่จาก spread** ตัวเลข `total_pnl` จึงไร้ความหมาย · ใส่กล่องบอกผู้อ่านตรง ๆ แล้วว่า mini-project นี้พิสูจน์ "โครงสร้าง" สำเร็จ แต่ไม่ได้พิสูจน์ "ผลตอบแทน" พร้อมโจทย์ต่อยอดให้ทำ Portfolio รับสองขา (งานรอบถัดไป)

**บทที่ 24 (Walk-Forward) — ไม่แสดง overfitting** · เจอ 2 บั๊ก:
1. **"out-of-sample" ไม่ได้ out-of-sample จริง** — comment เขียนว่า "200 วันสุดท้าย (ซึ่งไม่ได้รวมใน in-sample sweep)" แต่ `parameter_sweep` ใช้ **ทั้ง 1000 วัน** รวมท่อนทดสอบด้วย → แก้ให้ sweep บน `iloc[:800]` เท่านั้น
2. **ผมเองแก้ผิดจุดในรอบก่อน** — เปลี่ยน data เป็น OU (mean-reverting) ซึ่งทำให้ **มี edge จริง** พารามิเตอร์ที่ดีในอดีตจึงยังดีในอนาคต = **ไม่มีอะไรให้ decay** (ทดสอบแล้วทุกค่า `sigma` ให้ OOS ≥ IS) · การสาธิต overfitting **ต้องใช้ข้อมูลที่ edge เป็นของปลอม** ซึ่ง noise ล้วนแบบต้นฉบับถูกอยู่แล้ว — จูน `noise_scale=120` ได้ **IS Sharpe 1.99 → OOS −0.28** ตรงตามบทเรียนพอดี

> 🔑 **บทเรียนเชิงวิธีการ:** ข้อมูลจำลองต้องออกแบบตาม *สิ่งที่บทนั้นต้องการพิสูจน์* — บทที่ 22 ต้องการ edge จริง (จึงใช้ OU) แต่บทที่ 24 ต้องการ edge ปลอม (จึงใช้ noise) · **"แก้ให้เหมือนกันทุกบท" คือการแก้ผิด**

### ⏳ finding ที่เปิดไว้ (ยังไม่แก้ — ต้องตัดสินใจ)

| ที่ | อาการ | ทำไมยังไม่แก้ |
|---|---|---|
| **part6** | ✅ **รันได้แล้ว 27/28** — ทำ mock ของ `websockets`/`aiohttp`/`requests` (`scripts/mocks/sitecustomize.py`) + timeout 20 วิ/บล็อก · บล็อกที่ 28 คือ entry point ของระบบ live ที่รอ kill switch จึงรันไม่จบ**โดยตั้งใจ** ไม่ใช่บั๊ก · ⚠️ **ห้าม sync output ของ Part นี้** เพราะขึ้นกับ network/timing — ใส่หมายเหตุในเล่มแล้วว่าตัวเลขเป็นตัวอย่างรูปแบบ ไม่ใช่ค่าที่ต้องได้ตรงกัน |

| **part2:719, part0:329, part1:393/605** | fragment ที่อ้างตัวแปรจากเนื้อความ | ต้องตัดสินใจว่าจะทำให้ทุกบล็อก standalone หรือติดป้าย "ต่อจากบล็อกก่อน" |

---

## 8. แผน Phase R3 — ปิดหน้าผา scaffolding (ใบสั่งงาน)

### 8.1 แผนที่หน้าผา (วัดจริง)

นับบล็อกโค้ด `.fm` ที่ยาว ≥4 บรรทัด แล้วเช็คว่ามีกล่องอธิบาย (`.read-aloud` / `.ai-decode`) ติดภายใน 6 บรรทัดหลังบล็อกไหม:

| Part | บล็อก ≥4 บรรทัด | มี scaffolding | ครอบคลุม | บล็อกที่ยากสุด (คะแนน) |
|---|---|---|---|---|
| part1 | 41 | 14 | 34% | :974 (9) |
| part2 | 29 | **16** | **55%** | ✅ ทำแล้วใน R3 |
| part3 | 19 | 3 | **16%** | :577 (16), :866 (15) |
| part4 | 23 | 5 | 22% | :1302 (16), :179 (12) |
| part5 | 21 | 3 | **14%** | :1258 (20), :1088 (14) |
| part6 | 28 | 4 | 14% | **:1280 (42), :1867 (49)** |

> คะแนนความยาก = ผลรวมน้ำหนักของ construct ที่มือใหม่อ่านไม่ออกด้วยสายตา (comprehension, `await`, `asyncio.*`, decorator, `shift`+`cumsum`, `@` matmul, ฯลฯ)

**สิ่งที่ตารางนี้บอก — ความครอบคลุมแปรผัน *ผกผัน* กับความยาก:** Part V–VI มีบล็อกยากที่สุดของเล่ม (สูงสุด **239 บรรทัด**) แต่ครอบคลุมแค่ 14% ส่วน Part I ที่ง่ายสุดกลับมีมากที่สุด

> ⚠️ **แก้ตัวเลขจากฉบับแรกของแผนนี้:** เดิมรายงานว่า part4/5/6 = **0%** ซึ่ง**ผิด** — เกิดจากสคริปต์ `cliff-map.py` เองนับเฉพาะ `.read-aloud`/`.ai-decode` และมองหาภายใน 6 บรรทัดหลังบล็อกเท่านั้น จึงพลาด (ก) กล่อง `.bx bk`/`.bx ba` ที่หนังสือใช้เป็น scaffolding เหมือนกัน และ (ข) กรณีที่มี `.output` คั่นอยู่ · แก้เครื่องมือให้นับกล่องเตือน/กับดักด้วย และค้นหาจนถึงบล็อกโค้ดถัดไปแทนหน้าต่างตายตัวแล้ว
>
> **แต่ finding เรื่อง CSS ยังคงอยู่และสำคัญ:** part4/5/6 ไม่มี CSS ของ `.read-aloud`/`.ai-decode` จริง ๆ (ยืนยันแล้ว) — แปลว่ากลไก **"อธิบายระดับบรรทัด"** ใช้ไม่ได้เลยในสาม Part นั้น มีแต่กล่องเตือนระดับหยาบ · ซึ่งไม่พอสำหรับมือใหม่ที่ต้องการรู้ว่า*บรรทัดนี้*ทำอะไร (port CSS ให้แล้วใน R3 spike)

### 8.2 ปัญหาหนักกว่าที่คิด — บล็อกยักษ์

| ที่ | ขนาด | เนื้อใน |
|---|---|---|
| part6:1185 | **239 บรรทัด** | `async def`×7, `await`×8, `async with`×3 |
| part6:1772 | **221 บรรทัด** | `async def`×4, `await`×8, asyncio primitive×4 |
| part6:740 | **196 บรรทัด** | `async def`×4, `await`×7, `try/except`×6, `self.x=`×16 |
| part4:509 | **170 บรรทัด** | `self.x=`×18 (event-driven backtester) |
| part5:1406 | 83 บรรทัด | — |
| part4:1293 | 90 บรรทัด | rolling×3, cumulative×3 |

➡️ **กล่อง read-aloud แก้บล็อก 239 บรรทัดไม่ได้** — ของขนาดนี้ต้อง **ผ่าเป็นขั้น** ไม่ใช่ติดป้ายอธิบาย นี่คือเหตุที่ R3 ต้องมี scaffolding 4 ชนิด ไม่ใช่ชนิดเดียว

### 8.3 จุดเปิดตัว construct ครั้งแรก (แกนของแผน)

| construct | โผล่ครั้งแรก | มีกล่องอธิบายไหม |
|---|---|---|
| list comprehension | part1:524 | ✅ มี `.ai-decode` |
| `lambda` | part1:621 | ❌ |
| `adfuller()` | part2:271 | ❌ |
| `coint()` | part2:541 | ❌ |
| `minimize()` | part2:656 | ❌ |
| `@` (matmul) | part2:670 | ❌ |
| `linprog()` | part2:881 | ❌ |
| `@property` | part3:208 | ❌ |
| `super().` | part3:379 | ✅ มี `.ai-decode` |
| `@abstractmethod` | part3:464 | ✅ มี `.ai-decode` |
| `.groupby()` | part4:242 | ❌ |
| `shift`+`cumsum` นับ trade | part4:242 | ❌ |
| `@dataclass` | part4:448 | ❌ |
| `try/except` | part4:904 | ❌ |
| **`async def` + `await` + `asyncio.gather`** | **part6:155** | ❌ **ทั้งสามตัวเปิดตัวพร้อมกันในบล็อกเดียว** |
| `async with` + `yield` | part6:293 | ❌ |
| `asyncio.Queue` | part6:458 | ❌ |
| `asyncio.create_task` | part6:548 | ❌ |

🔴 **จุดที่แย่ที่สุดในเล่มทั้งเล่ม = part6:155** — มือใหม่เจอ 3 แนวคิดใหม่หมด (`async def`, `await`, `asyncio.gather`) พร้อมกันในบล็อกเดียว โดยไม่มีคำอธิบายใดเลย

### 8.4 กฎคัดเลือก — เติมที่ไหน (ไม่เติมทุกที่)

บล็อก ≥4 บรรทัดมี ~161 บล็อก · เติมทุกบล็อก = หนังสือบวมและมือโปรเบื่อ ใช้ 3 กฎนี้ตัดสิน:

- **R-1 · กฎเปิดตัวครั้งแรก (สำคัญสุด)** — construct ใดโผล่**ครั้งแรก** ต้องมีกล่องอธิบาย **เสมอ** · ครั้งที่ 2 ขึ้นไป **ห้ามอธิบายซ้ำ**
  > 🔑 นี่คือกลไกที่ทำให้ "มือใหม่ตามได้" กับ "มือโปรไม่เบื่อ" อยู่ร่วมกันได้: `await` ถูกอธิบายละเอียดครั้งเดียวที่ part6:155 หลังจากนั้นทั้งเล่มถือว่าผู้อ่านรู้แล้ว มือโปรข้ามกล่องเดียว ไม่ใช่ข้าม 30 กล่อง
- **R-2 · เกณฑ์ความยาก** — บล็อกคะแนน ≥8 ต้องมี scaffolding
- **R-3 · เกณฑ์ขนาด** — บล็อก >40 บรรทัด ต้อง**ผ่า** (Type C) ไม่ใช่แค่ติดกล่อง

### 8.5 scaffolding 4 ชนิด — ใช้อันไหนเมื่อไร

| ชนิด | ใช้เมื่อ | รูปแบบ | งบคำ |
|---|---|---|---|
| **A · 🚶 `.read-aloud`** | บล็อกสั้น-กลางที่มี construct ยาก 1–2 ตัว | "อ่านบรรทัดนี้ว่า: …" แปลเป็นภาษาคน + ชี้กับดัก 1 จุด | ≤2 ประโยค |
| **B · 🤖 `.ai-decode`** | one-liner / บรรทัดที่อ่านไม่ออกด้วยสายตา (comprehension, chained pandas) | หัวข้อ + โค้ด + **สูตรอ่าน** ("อ่านครึ่งหลังก่อน") + `<ol>` ทีละขั้น + สรุป 1 บรรทัด | ≤5 ข้อ |
| **C · 🧱 ผ่าบล็อก (block surgery)** | บล็อก >40 บรรทัด | ① กล่อง "โครงก่อนโค้ด" (แผนผัง 3–5 ขั้นว่าคลาสนี้ทำอะไร) → ② ผ่าโค้ดเป็น 2–4 บล็อกย่อยตามหน้าที่ → ③ read-aloud สั้นคั่นแต่ละขั้น → ④ บล็อกรวมท้าย (ถ้าจำเป็น) | — |
| **D · 🔵 `[รอบสอง]` มือโปร** | ทุกบทต้องมี ≥1 | trade-off / "ทำไมไม่ทำแบบอื่น" / กับดักที่มือโปรก็พลาด / ตัวเลขจริง | ≤1 ย่อหน้า |

**Template Type A** (ยึด voice จาก Part I ที่มีอยู่แล้ว):
> `<div class="read-aloud">อ่าน <code>…</code> ว่า: "…" — <strong>…</strong> · ⚠️ กับดัก: …</div>`

**Template Type C ①** (กล่องโครงก่อนโค้ด):
> `<div class="bx bb"><div class="bt">🧱 โครงของคลาสนี้ก่อนดูโค้ด</div><ol><li>…</li></ol></div>`

### 8.6 เป้าหมายต่อ Part (จำนวนกล่องที่ต้องเขียน)

| Part | A 🚶 | B 🤖 | C 🧱 ผ่า | D 🔵 | รวม | จุดสำคัญ |
|---|---|---|---|---|---|---|
| part1 | 2 | 1 | 0 | 1 | 4 | :974 (loc/iloc×4), :621 (`lambda` เปิดตัว) |
| part2 | 6 | 2 | 2 | 2 | 12 | :541 `coint()`, :656 `minimize()`, :670 `@`, :783/:903 ผ่า |
| part3 | 5 | 1 | 2 | 2 | 10 | :208 `@property`, :577/:866 ผ่า |
| part4 | 5 | 2 | 3 | 2 | 12 | :242 `shift`+`cumsum`, :448 `@dataclass`, :509/:1293 ผ่า |
| part5 | 4 | 1 | 2 | 2 | 9 | :1249, :1406 ผ่า |
| **part6** | **7** | **2** | **3** | **2** | **14** | **:155 async เปิดตัว 3 ตัว · :740/:1185/:1772 ผ่า** |
| รวม | 29 | 9 | 12 | 11 | **~61** | — |

### 8.7 ลำดับทำ + เหตุผล

1. **Spike: part6:155 + part6:740 ก่อน** — ทำจุดที่ยากสุดก่อนเพื่อ **ทดสอบ template Type C กับเคสหนักสุด** ถ้า template เอาบล็อก 196 บรรทัดอยู่ ที่เหลือก็เอาอยู่ · ถ้าทำ Part II ก่อนแล้วเจอว่า template ไม่พอตอนถึง Part VI จะต้องรื้อย้อนหลัง
2. **แล้วไล่ part2 → part3 → part4 → part5 → part6 (ที่เหลือ) → part1** ตามลำดับที่ผู้อ่านเดิน เพื่อให้กฎ R-1 (เปิดตัวครั้งแรก) นับถูก — ต้องรู้ว่า construct ไหนอธิบายไปแล้ว
3. **ปิดท้าย: reading-path banner 🟢/🔵 ทุก Part** — ทำหลังสุดเพราะต้องรู้ว่าแต่ละ Part มีกล่องชั้นไหนบ้างจึงเขียน banner ได้ตรง

commit แยกต่อ Part: `docs(python): เติมชั้นอธิบาย Part N — A×n B×n C×n D×n`

### 8.8 กันหนังสือบวม (ข้อบังคับ)

- `.read-aloud` **≤2 ประโยค** · `.ai-decode` **≤5 ข้อ** — เกินนั้นคือสัญญาณว่าควรผ่าบล็อกแทน (Type C)
- **ห้ามอธิบาย construct ซ้ำ** (กฎ R-1) — ถ้าอยากย้ำ ให้ลิงก์กลับจุดที่อธิบายไว้แล้ว
- กล่องสีติดกัน **≤4 กล่อง** ต่อหัวข้อ (ตาม style guide) — เกินนั้นยุบเป็นร้อยแก้ว
- Type C **ห้ามลดโค้ด** — ผ่าเป็นบล็อกย่อยได้ ตัดบรรทัดออกไม่ได้ (กฎ "เพิ่มได้ ห้ามลด")
- ทุกกล่องที่เพิ่ม **ต้องไม่แตะโค้ด** — ถ้าต้องแก้โค้ดด้วย แยกเป็น finding ของ R1/R2

### 8.8b ⚠️ ข้อบกพร่องคนละชั้นที่เพิ่งค้นพบ — `.output` ไม่ตรงกับโค้ด

R1 ตรวจแค่ว่า **"โค้ดรันได้ไหม"** แต่ไม่ได้ตรวจว่า **"ผลลัพธ์ที่หนังสือเขียนไว้ตรงกับที่โค้ดพิมพ์จริงไหม"** — เป็นคนละชั้นกัน และชั้นหลังอันตรายกว่าสำหรับผู้อ่าน เพราะโค้ดรันผ่านแต่ได้ตัวเลขต่างจากหนังสือ → ผู้อ่านจะคิดว่า**ตัวเองทำพลาด**

สร้างสคริปต์ `check-output.py` เทียบ `.output` กับ stdout จริง พบ:

| Part | จุดที่ไม่ตรง | สถานะ |
|---|---|---|
| part1 | 2 | ✅ → 1 (ที่เหลือคือหนังสือย่อด้วย `...` โดยตั้งใจ) |
| part2 | 1 | ✅ → 0 |
| part3 | 2 | ✅ → 0 |
| **part4** | **16** | ✅ → 0 |
| **part5** | **15** | ✅ → 1 (ที่เหลือคือ artifact ของ harness) |
| **รวม** | **36** | ✅ **→ 2 ที่อธิบายได้ว่าไม่ใช่บั๊ก** |

**ต้นเหตุหลักไม่ใช่ "ตัวเลขเก่า" แต่เป็นบั๊กข้อมูลแบบเดียวกับบทที่ 13:** Part IV สร้าง spread ด้วย `binance = bybit + noise` (white noise) → กลยุทธ์ mean-reversion ไม่มี edge ตั้งแต่ต้น · แก้เป็น OU process แล้วบทที่ 25 (cost sensitivity) กลับมาทำงานสวยงาม: Sharpe 1.90 → 0.57 → 0.00 → −1.06 ไล่ตามระดับต้นทุนพอดี

**และพบข้อบกพร่องเชิงตรรกะที่ลึกกว่า** — ตัวอย่าง lookahead bias ในบทที่ 22 **พิสูจน์สิ่งที่ตัวเองอ้างไม่ได้**: สำหรับ signal แบบ mean-reversion การไม่ `shift(1)` ทำให้ผล *แย่ลง* ไม่ใช่ดีขึ้น (เพราะ signal ถูกจุดชนวนโดยการเคลื่อนไหวที่เพิ่งเกิดในแท่งนั้นเอง) ตัวเลข "ghost profit +12.66%" จึงเป็นไปไม่ได้จากโค้ดนั้น · แก้โดย **เพิ่มหัวข้อ 22.1b** ที่สาธิต lookahead ตัวจริงซึ่งทำให้ผลดูดีขึ้นได้จริง — full-sample statistics (`spread.mean()` แทน `.rolling().mean()`) → ghost profit **+3.36%** ยืนยันด้วยการรันจริง · พร้อมยกระดับเป็นบทเรียนที่ดีกว่าเดิม: *"ไม่ใช่ทุก lookahead ที่ทำให้ผลดูดีขึ้น — แต่ทุก lookahead ทำให้ผลไม่จริง"*

➡️ **เพิ่มเข้าเกณฑ์ตรวจจบ:** ทุก Part ต้องผ่าน `check-output.py` = 0 จุด

### 8.9 เกณฑ์ "R3 จบ" ต่อ Part

- [ ] ทุก construct ในตาราง §8.3 ที่เปิดตัวใน Part นี้ มีกล่องอธิบายที่จุดเปิดตัว
- [ ] ไม่มีบล็อก >40 บรรทัดที่ไม่ผ่านการผ่าหรือไม่มีกล่อง "โครงก่อนโค้ด"
- [ ] บล็อกคะแนนยาก ≥8 มี scaffolding ครบ
- [ ] มี `[รอบสอง]` ชั้นมือโปร ≥1 กล่อง
- [ ] มี reading-path banner 🟢/🔵
- [ ] รันสคริปต์ `cliff_map.py` ซ้ำ → ความครอบคลุมของ Part นั้น ≥60% และไม่มีบล็อกคะแนน ≥8 ที่ยังว่าง

---

## 9. ผล Phase R3 รอบ Part V — และ **เครื่องมือวัดที่วัดผิดมาตลอด**

### 9.1 🔧 แก้ `cliff-map.py` — ตัวเลขความครอบคลุมก่อนหน้านี้ต่ำกว่าความจริง

เจอ bug 2 ข้อในตัววัดเอง ซึ่งแปลว่า **ตัวเลข "มี scaffolding แล้ว: N" ทุกตัวที่รายงานไว้ก่อนหน้านี้ต่ำเกินจริง**

| # | อาการ | สาเหตุ | ผลที่ตามมา |
|---|-------|--------|------------|
| 1 | กล่องที่มีอยู่แล้วไม่ถูกนับ | `SCAFF` เขียนเป็น `bx b[kabp]` ระบุสีตายตัว → `br` (แดง = "bug ที่ซ่อนอยู่") · `bg` (เขียว = "แบบที่ถูก") · `bd` (เทา = สรุป) หลุดหมด | บล็อกที่อธิบายไว้ครบแล้วถูกรายงานว่าว่าง → เกือบไปเติมกล่องซ้ำ |
| 2 | นับเฉพาะ scaffolding ที่อยู่ **หลัง** บล็อก | หน้าต่างตรวจใช้แค่ `lines[end:end+40]` | หนังสือใช้ `read-aloud` วางไว้**ก่อน**บล็อกเป็นปกติ (รวมทั้งที่เพิ่งเติมเอง) → ไม่ถูกนับเลย |

แก้แล้วทั้งคู่ (`b\w+` + หน้าต่างสองฝั่ง โดยตัดขอบที่บล็อกโค้ด/หัวข้อก่อนหน้า และจำกัด 15 บรรทัดสุดท้ายกันการนับกล่องของบล็อกอื่นมาเป็นของตัวเอง)

**ผลการวัดใหม่ทั้งเล่ม (หน้าผาจริงอยู่คนละที่กับที่เคยเข้าใจ):**

| Part | ครอบคลุม | คะแนนยากสูงสุดที่ยังว่าง | สถานะ |
|------|----------|--------------------------|--------|
| 0 | 0/2 | 2 | บล็อกสั้นมาก ไม่ต้องทำ |
| I | 26/48 | 6 | ที่เหลือคะแนน ≤6 — พอแล้ว |
| II | 18/29 | 9 | ปานกลาง |
| III | 7/21 | **15** | ⚠️ OOP — ยังเป็นหน้าผา |
| IV | 8/24 | **16** | ⚠️ ยังเป็นหน้าผา |
| V | **20/27** | 8 | ✅ ผ่านเกณฑ์ §8.9 แล้ว |
| VI | 8/28 | **36** | 🔴 หน้าผาที่สูงที่สุดในเล่ม (async) |

> **ลำดับงานที่เหลือเรียงใหม่ตามนี้: VI → III → IV → II**
> เดิมเข้าใจว่า Part V แย่ที่สุด (6/25) ซึ่งเป็นผลจาก bug ข้อ 1+2 ล้วน ๆ

### 9.2 🔴 บั๊กร้ายแรงที่สุดที่เจอทั้งการตรวจ — บทที่ 30 walk-forward ไม่เคยทำงานเลย

**อาการ:** ทุก fold คืน `sharpe_test = NaN` และ `n_trades_test = 0` — โค้ดไม่ error แม้แต่บรรทัดเดียว

**สาเหตุ:** `walk_forward_validation` คำนวณ `compute_vol_zscore` บน **test window ล้วน (63 แถว)** แต่
- `ann_vol` ใช้ `rolling(21, min_periods=21)` → กิน 20 แถวแรกเป็น NaN เหลือ 43 แถว
- `zscore_window=63` ต้องการ 63 แถวที่ไม่ NaN → **ไม่มีทางถึง**
- → `vol_zscore` เป็น NaN ทั้งชุด → ไม่มีสัญญาณ → ไม่มีเทรด → Sharpe = NaN

**ความรุนแรงเพิ่มเติม — ตัวเลขในหนังสือเป็นของที่แต่งขึ้น:** กล่องตีความเขียนว่า *"Mean test Sharpe 0.57 · % folds > 0 = 100% · Std 0.09 · 2-4 trades/quarter"* และ docstring ของคลาสปิดบทเขียนว่า *"Validated — Walk-forward: mean Sharpe 0.57, 100% positive folds"* ทั้งที่โค้ดคืน NaN ทุกช่อง · ตัวเลข `0.74` / `0.57` ไม่เคยออกมาจากการรันจริง

**แก้แล้ว 3 ชั้น:**
1. **โค้ด** — คำนวณ feature บน `prices.iloc[start:test_end]` (train+test ต่อกัน) แล้วค่อยตัด `res_hist.iloc[train_size:]` มาวัดเฉพาะช่วง test · ถูกต้องกว่าเดิมเชิงหลักการด้วย เพราะวันแรกของ test ในชีวิตจริง**มีข้อมูล train อยู่ในมือแล้ว** การทิ้งไม่ใช่การกันมองอนาคต · ยังไม่ lookahead เพราะ rolling ทุกตัวเป็น `center=False` · เพิ่ม `start_equity=res_test["equity"].iloc[0]` กัน `annual_return` เพี้ยน
2. **ตัวเลข** — sync จากการรันจริง: mean test Sharpe `-0.752` · std `2.089` · ผ่าน 38% ของ fold · **5 เทรดใน 2 ปี**
3. **เรื่องเล่า** — เขียนกล่องตีความใหม่ทั้งหมดให้ตรงผล และ**เปลี่ยนบทสรุปจาก "deploy-ready" เป็น "ตกรอบ"**

**เหตุผลที่ไม่แก้ด้วยการเปลี่ยน seed/ข้อมูลให้ผลออกมาสวย:** ราคาทั้งบทสร้างจาก `np.random.normal` = random walk บริสุทธิ์ → **"ไม่มี edge" คือคำตอบที่ถูกต้องเพียงคำตอบเดียว** · การหมุน seed จนได้ Sharpe บวกคือ p-hacking ซึ่งเป็นบาปที่บทที่ 28 เตือนไว้ทั้งบท · จึงเขียนบทให้จบด้วยการ**ปฏิเสธกลยุทธ์** พร้อมอธิบายว่าทำไมนั่นคือการทำงานที่ถูกต้องของ walk-forward

> เข้ากฎเดิมที่บันทึกไว้ตอนแก้ ch24: *ข้อมูลจำลองต้องออกแบบตามสิ่งที่บทนั้นต้องการพิสูจน์* — และในกรณีนี้สิ่งที่บทต้องการพิสูจน์คือ "เครื่องมือตรวจต้องกล้าบอกว่าไม่ผ่าน"

### 9.3 🟠 บั๊กที่ 2 ในบทที่ 30 — backtest หน่วง 2 แท่ง ทั้งที่ prompt สั่ง 1 แท่ง

หน่วงซ้อนกันสองรอบโดยไม่ตั้งใจ: ลูป `for` อ่าน `signal.iloc[i-1]` มาตั้ง `position.iloc[i]` (รอบ ①) แล้ว `df["position"].shift(1) * log_ret` หน่วงอีก (รอบ ②) · **แต่ละบรรทัดแยกกันดูถูกทั้งคู่** และห่างกัน 15 บรรทัด จึงรอดสายตา audit เดิม

ไม่อันตราย (อนุรักษ์นิยมเกิน ตรงข้ามกับ lookahead) แต่ผิดจากที่สั่ง · **ไม่แก้โค้ด** เพราะบทนี้คือบท "audit โค้ดที่ AI เขียน" — เก็บไว้เป็นของจริงให้ตรวจเจอ · เพิ่มบล็อก **impulse response** (ราคาขึ้นวันละ 1% คงที่ + ยิงสัญญาณครั้งเดียว → นับหน่วงได้ด้วยตา) และอธิบายว่าทำไม embargo test ที่มีอยู่จึงมองข้อนี้ไม่เห็น (มันตอบแค่ "แตะอนาคตไหม" ไม่ได้ตอบ "หน่วงกี่แท่ง")

### 9.4 🟡 บทที่ 29 — corner solution ที่ output แสดงอยู่แล้วแต่ไม่เคยอธิบาย

`optimize_sharpe` คืน **BTC 0% · ETH 0% · SOL 100%** ทั้งที่โจทย์คือกระจายความเสี่ยง 3 สินทรัพย์ · โค้ดถูกทุกบรรทัด ปัญหาอยู่ที่ input

เพิ่มบล็อกเทียบ "ค่าจริงที่ใช้สร้างข้อมูล" กับ "ค่าที่วัดได้จาก 252 วัน":

| | จริง | วัดได้ | ±คลาดเคลื่อน |
|---|---|---|---|
| BTC | +12.6% | **−22.5%** | 22.4% |
| ETH | +17.6% | **−47.5%** | 27.5% |
| SOL | +25.2% | +33.4% | 35.5% |

**ผิดกระทั่งเครื่องหมาย** — ขณะที่ความผันผวนวัดได้แม่นมาก (22.4 → 21.3) จากข้อมูลชุดเดียวกัน · เหตุผลเชิงหลักการ: sd ประมาณจาก "ขนาดการแกว่ง" ซึ่งมีให้เก็บทุกแท่ง ส่วนค่าเฉลี่ยประมาณจาก "ระยะทางสุทธิต้นถึงปลาย" ซึ่งมีค่าเดียวไม่ว่าเก็บถี่แค่ไหน → **เก็บข้อมูลรายนาทีไม่ช่วยให้รู้ค่าเฉลี่ยแม่นขึ้น มีแต่ต้องรอนานขึ้น** (ต้องใช้ 18–29 ปี) · ปิดท้ายด้วยทางออกที่ใช้จริง 3 แนว (ตัดค่าเฉลี่ยออก / บีบ `max_weight` / shrinkage)

### 9.5 สรุปสถานะ Part V

- scaffolding **20/27** (เกณฑ์ §8.9 คือ ≥60% → **ผ่าน**) · ไม่มีบล็อกคะแนน ≥8 ที่ยังว่าง ยกเว้น 2 บล็อกที่มีกล่องเตือนของเดิมครอบอยู่แล้ว
- รันโค้ด **22/24** — 2 ที่พังคือ `inspect.getsource` บนโค้ดที่ `exec` (false positive ที่บันทึกไว้แล้วใน §7)
- `.output` ตรงกับการรันจริงทุกบล็อก (เหลือ 1 = บล็อก `inspect` ตัวเดิม)
- `<div>` สมดุล 174/174
