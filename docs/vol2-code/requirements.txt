# Practical Quant: Stat Arb — vol2-code requirements
# Tested with Python 3.11. Install: pip install -r requirements.txt
numpy
pandas
scipy
statsmodels
matplotlib
scikit-learn      # PCA (notebook 03)
pykalman          # Kalman filter (notebook 01; also used ahead in Part IV-V notebooks)

# to actually execute the .ipynb files from the command line (not required just to read them):
nbformat
nbclient
ipykernel

# NOT required for notebooks 01-03 (we hand-rolled a Gaussian-copula demo instead) but
# recommended if you want to go further with Clayton/Gumbel/Student-t copulas or the
# ready-made Mispricing Index strategy that Part II references:
#   pip install arbitragelab
