node_modules/

# MinerU (kept in its own folder, separate from the JS project)
mineru/.venv/
mineru/output/

# pdf-to-markdown skill (PyMuPDF-based MinerU fallback)
.claude/skills/pdf-to-markdown/.venv/
