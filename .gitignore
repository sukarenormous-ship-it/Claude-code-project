node_modules/

# MinerU (kept in its own folder, separate from the JS project)
mineru/.venv/
mineru/output/

# pdf-to-markdown tool (PyMuPDF-based MinerU fallback, own self-contained folder)
pdf-to-markdown/.venv/
pdf-to-markdown/output/
